/** @type {import('next').NextConfig} */
// Nothing to configure yet — the defaults are what this site wants. The app
// lives at src/app, which Next finds on its own; public/ stays at the root.
const nextConfig = {}

export default nextConfig
